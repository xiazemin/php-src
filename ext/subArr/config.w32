// $Id$
// vim:ft=javascript

// If your extension references something external, use ARG_WITH
// ARG_WITH("subArr", "for subArr support", "no");

// Otherwise, use ARG_ENABLE
// ARG_ENABLE("subArr", "enable subArr support", "no");

if (PHP_SUBARR != "no") {
	EXTENSION("subArr", "subArr.c", PHP_EXTNAME_SHARED, "/DZEND_ENABLE_STATIC_TSRMLS_CACHE=1");
}

